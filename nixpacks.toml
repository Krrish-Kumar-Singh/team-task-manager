# Railway/Nixpacks — pin Node 22 LTS (avoid Node 23 EOL / Python mismatches)
[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
