# Fallback only if Dockerfile build is disabled. Prefer Dockerfile on Railway.
[variables]
NIXPACKS_NODE_VERSION = "22"

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
